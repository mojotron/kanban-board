import { describe, test, expect } from 'vitest';
import { render } from '@testing-library/react';
import Task from './Task';

describe('KanbanBoar page - Task component', () => {
  test('happy path', () => {});
});
